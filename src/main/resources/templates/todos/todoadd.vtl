<!DOCTYPE html>
<html>
<title>Add a ToDo!</title>
<style>
    #titlething {
  color: #FFFFFF;
  font-size: 50px;
  text-align: center;
  border: 5px solid #FFFFFF;
  margin: 0;
  padding: 10px;
}
body{
background-image:linear-gradient(240DEG, #3B5998, #add8e6);
height:800px;
}

.container {
  max-width: 450px;
  margin: 0 auto;
  border-top: : 1px solid #FFFFFF;
  background-color: rgba(255, 255, 255, .2);
  box-shadow: 0 0 20px rgba(0, 0, 0, .1);
  user-select: none;
  text-align: center;
}
#addbutton{
  width: 145;
  height: 59;
  background-image:linear-gradient(240DEG, #3B5998, #add8e6);
  color:white;
  font-size:18px;
  cursor:pointer;
  outline:none;
  margin-top: 15px;
  text-align: center;
}

</style>
<head>
    <link type="text/css" rel="stylesheet" href="../addstyle.css"/>
</head>
<div class="container">
    <div id="titlething">Add a ToDo!</div>
    <form action="/todos/new" method="POST">
    <input class="form" id="newtodo" name="content" type="text"/>
    <input type="hidden" name="userid" value="$user.id">
    <input id="addbutton" name="task" type="submit" class="btn btn-default" value="Add" />
    <select name="labels">
        #foreach( $label in $labels )
            <option value=$label.id>$label.name</option>
        #end
    </select>
</form>

</div>

</html>
